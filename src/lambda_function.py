

def lambda_handler(event, context):
    # This function needs to do the following every six hours:
    # 1) It will scrape and save the 1,000 top projects on Github
    # 2) Then, it will get the desired information for all these projects
    # 3) Finally, it will push the desired data to the database
    return
